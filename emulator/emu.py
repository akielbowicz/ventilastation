from itertools import cycle
#import pygameengine
import pygletengine

# from asciiart import AsciiArt

# engine = AsciiArt()

# c1 = [0, 255, 255, 255]
# c2 = [255, 0, 255, 255]
# c3 = [104, 121, 99, 255]

# test_input = [
#     [c1, c2, c2, c2, c3, c3],
#     [c1, c2, c2, c3, c3, c3],
#     [c1, c2, c2, c2, c3, c3],
#     [c1, c1, c2, c2, c2, c3],
#     [c1, c1, c1, c2, c2, c3],
#     [c1, c1, c1, c2, c2, c3],
#     [c1, c1, c2, c2, c2, c3],
# ]

# for line in cycle(test_input):
#     engine.draw(line)
